# Web Framework
fastapi
uvicorn[standard]
python-multipart  # required by FastAPI's UploadFile (P7 onboarding: map/camera-image uploads); no transitive deps

# Database
sqlalchemy
alembic
psycopg[binary]  # PostgreSQL driver; only imported/connected to when DATABASE_URL is a postgresql:// URL

# Validation
pydantic
pydantic-settings

# Authentication (P4.4)
pyjwt
bcrypt

# Logging
structlog

# Linting & Formatting (Development)
ruff

# Testing (Development)
pytest
httpx  # required by fastapi.testclient.TestClient

# CCTV Video Processing
ultralytics
opencv-python
numpy
